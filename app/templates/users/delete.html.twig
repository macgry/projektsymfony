{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.user_delete'|trans }} {{ user.id|default('') }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.user_delete'|trans }} {{ user.id|default('') }}</h1>
    {{ form_start(form, { method: 'DELETE', action: url('user_delete', {id: user.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.delete'|trans }}" class="btn btn-danger" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('user_index') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}
